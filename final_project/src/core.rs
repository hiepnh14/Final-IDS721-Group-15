pub fn generator(prompt: String) -> Result<String, Box<dyn std::error::Error>> {
    // Imagine this function calls some AI service or does complex processing
    Ok(format!("Processed: {}", prompt))
}
